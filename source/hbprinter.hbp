#
# $Id: hbprinter.hbp,v 1.7 2016-12-10 18:52:26 fyurisich Exp $
#

# build a static library
-hblib

# compilation is incremental (hidden .hmk folder is created)
# to make a full compilation add '-rebuild' flag to command line
-inc

# name of output file
-o${HG_ROOT}\${LIB_GUI}\${hb_name}

# path to headers files (.ch)
-i${HG_ROOT}\include

# harbour compiler flags
-n -q0 -ko -w3 -es2

# uncomment this line to build .ppo files
#-p${HG_ROOT}\source\ppo\

# files to compile
winprint.prg

# EOF
