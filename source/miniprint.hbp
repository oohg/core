#
# $Id: miniprint.hbp,v 1.5 2013-08-22 22:25:08 fyurisich Exp $
#

# compilation is incremental (hidden .hmk folder is created)
-inc

# build a static library
-hblib

# name of output file
-o${HG_ROOT}\lib\hb\mingw\${hb_name}
#-o${HG_ROOT}\lib\hb\mingw64\${hb_name}

# harbour compiler flags
-n -q0 -ko -w3 -es2

# uncomment this line to build .ppo files
#-p${HG_ROOT}\source\ppo\

# path to headers files (.ch)
-i${HG_ROOT}\include

# files to compile
miniprint.prg

# EOF
